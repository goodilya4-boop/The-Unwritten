package com.theunwritten.character.data;

/**
 * Fundamental character attributes. Values are stored as doubles so future
 * systems can apply fractional modifiers without changing the data model.
 */
public enum AttributeType {
    STR,
    DEX,
    END,
    INT,
    WIL,
    PER
}
